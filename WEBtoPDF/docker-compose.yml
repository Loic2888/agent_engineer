version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3002:3002"
    env_file: .env
    volumes:
      - ./backend/uploads:/app/uploads

    restart: unless-stopped
