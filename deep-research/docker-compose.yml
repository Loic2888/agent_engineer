services:
  backend:
    build:
      context: ./backend
    env_file: .env
    environment:
      CORS_ORIGINS: "http://localhost"
      DB_PATH: "/data/research.db"
    volumes:
      - research_data:/data
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  research_data:
