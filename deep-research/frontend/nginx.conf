server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

<<<<<<< HEAD
    # Proxy API + SSE vers le backend — buffering désactivé pour le streaming
=======
    # Proxy SSE — buffering désactivé pour le streaming
>>>>>>> 2c1d62c3fabcf807dbf85dc1033ab2ce2b94b59d
    location /research {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        add_header X-Accel-Buffering no;
    }
<<<<<<< HEAD
=======

    # Proxy REST — historique des recherches
    location /history {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Health check
    location /health {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
    }
>>>>>>> 2c1d62c3fabcf807dbf85dc1033ab2ce2b94b59d
}
