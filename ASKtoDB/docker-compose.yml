services:
  backend:
    build: ./backend
    ports:
      - "8005:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3005:3000"
    depends_on:
      - backend
    restart: unless-stopped
